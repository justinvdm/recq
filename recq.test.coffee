describe "recq", ->
  describe "the request", ->
    it "should use basic auth if given"
    it "should use the request body if given"
    it "should set the content type to json if relevant"
    it "should use the given request method"
    it "should use the given request url"

  describe "the output", ->
    it "should append the request data to the data file"
    it "should append the response data to the data file"
